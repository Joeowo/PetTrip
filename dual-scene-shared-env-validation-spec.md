# 双场景共享环境生产路线验证 Spec v0.1

## Problem Statement

PetTrip 首个默认目的地需要生成**两个场景**：它们共享同一个视觉环境（背景、建筑、地标、光照、构图、画风），但宠物处于不同的行为和状态（例如：宠物在灯塔下休息 vs. 宠物在灯塔旁探索）。

当前未知量不是"图片模型能不能生成单张好看的场景"，而是：

```text
场景 A（环境 + 宠物状态1）与 场景 B（环境 + 宠物状态2）
能否在产品可接受的一致性水平下共享同一个环境？
```

**环境一致性**不等于逐像素相同。它意味着：
- 同一地标（灯塔、桥、树）在两个场景中保持相同的形状、位置、尺度、视角
- 同一光照条件（时间、天气、主光方向、色温）
- 同一画风、色板、材质处理
- 同一构图框架（地平线、前中远景分层）

但允许的变化包括：
- 宠物位置、姿态、动作
- 与宠物互动相关的局部细节（例如宠物踩过的草地、靠近的物体）
- 动态元素的微小变化（云、波浪、树叶）

本 Spec 探索并验证四条候选生产路线，通过真实样本和测量数据回答：哪条路线在当前技术条件下最有希望达到产品可接受的环境一致性。

## Solution

验证使用 4 个固定目的地场景概念（灯塔海岸、森林空地、山谷桥梁、湖畔码头），每个概念测试四条候选生产路线，每条路线生成两个场景（宠物状态不同）。

**候选路线**：

### 路线 A：纯环境母版 + 局部宠物编辑

1. 生成一张无宠物的纯环境图（纯场景母版）
2. 使用 Mask + inpainting 在母版上生成宠物状态1 → 场景 A
3. 使用 Mask + inpainting 在同一母版上生成宠物状态2 → 场景 B

**假设**：母版完全相同保证环境一致性；宠物通过局部编辑注入。  
**风险**：inpainting 可能改变 Mask 外的背景；两次 inpainting 的宠物可能风格不一致。

### 路线 B：完整场景 A 作为参考生成场景 B

1. 生成完整场景 A（环境 + 宠物状态1）
2. 使用场景 A 作为参考图 + Prompt 描述宠物状态2 → 生成场景 B

**假设**：参考图约束保持环境一致，Prompt 引导宠物变化。  
**风险**：模型可能过度保留宠物状态1，或过度重绘整个场景。

### 路线 C：程序化 Mask + 独立角色生成

1. 生成纯环境母版
2. 程序绘制固定尺寸 Mask 在位置1 → image-2 生成宠物状态1 → 场景 A
3. 程序绘制固定尺寸 Mask 在位置2 → image-2 生成宠物状态2 → 场景 B

**假设**：程序化 Mask 确定性高；image-2 服从 Mask 约束。  
**风险**：pilot4mvp3 已证明 image-2 的固定尺寸 Mask 和严格边界控制不稳定。

### 路线 D：双场景同 Prompt 批次生成

1. 使用结构化 Prompt 同时描述两个场景，要求共享环境但宠物行为不同
2. 在一次调用或紧密批次中生成场景 A 和场景 B

**假设**：Prompt 语义约束能引导一致性。  
**风险**：没有显式共享母版，一致性完全依赖模型理解。

## User Stories

1. 作为视觉验证人员，我希望看到每条路线的两个场景并排展示，以便直观判断环境是否一致。
2. 作为视觉验证人员，我希望看到自动测量的地标位置偏移、颜色漂移和结构相似度，以便量化一致性。
3. 作为 Agent 技术开发者，我希望每条路线记录生成步骤、模型调用次数和失败模式，以便评估生产复杂度。
4. 作为 Agent 技术开发者，我希望验证"纯环境母版"是否真的能被模型生成，以便判断路线 A 和路线 C 是否可行。
5. 作为美术评审，我希望判断宠物与环境的融合感，以便区分"技术上一致"和"视觉上可接受"。
6. 作为项目负责人，我希望知道每条路线的成功率和最佳样本质量，以便选择主路线和降级方案。
7. 作为后续开发人员，我希望看到失败样本和失败分类，以便理解边界条件。

## Implementation Decisions

### 验证样本

使用 4 个目的地概念：

| 概念 | 环境描述 | 宠物状态1 | 宠物状态2 |
|---|---|---|---|
| 灯塔海岸 | 黄昏海岸，远处灯塔，海蚀岩石 | 宠物在岩石上休息 | 宠物在海边探索 |
| 森林空地 | 午后森林空地，阳光透过树冠 | 宠物坐在树桩上 | 宠物在草地上奔跑 |
| 山谷桥梁 | 清晨山谷，木桥跨越溪流 | 宠物站在桥头 | 宠物在桥中央张望 |
| 湖畔码头 | 黄昏湖泊，木质码头延伸入水 | 宠物在码头尽头 | 宠物在岸边戏水 |

每个概念 × 4 条路线 = 16 组实验，每组生成 2 个场景 = 32 张场景图。

### 统一画风与角色

所有场景使用 **Neva 画风**参考（同 pilot4mvp3）：手绘 2D 绘本感、粉彩块面、清晰剪影、横版 16:9、电影化构图。

角色统一为 **MYGO!!!!! 若叶睦 Q 版形象**（同 pilot4mvp3）：灰蓝短发、青绿色眼睛、Q 版头身比。

### 一致性指标

#### 1. 地标位置稳定性
- 标注两个场景中的主要地标（灯塔、桥、树等）
- 计算归一化坐标的欧氏距离
- 阈值：Δ < 0.05（5% 画布宽度）为稳定

#### 2. 结构相似度（SSIM）
- 对两个场景计算全局 SSIM
- 阈值：SSIM > 0.75 为高一致性（考虑宠物占比）

#### 3. 色板漂移
- 提取两个场景的主色板（前 5 色）
- 计算色彩距离（Lab 空间）
- 阈值：ΔE < 15 为可接受

#### 4. 人工一致性评分（1-5 分）
- 地标身份：同一灯塔/桥/树是否可辨认为同一物体
- 视角与构图：相机位置、地平线、透视是否一致
- 光照与氛围：时间、天气、主光方向是否一致
- 画风与材质：笔触、块面、色板是否一致
- 整体可接受性：作为"同一目的地的两个场景"是否成立

### 实验单元命名

```
Cxx-Ry-SceneA.png
Cxx-Ry-SceneB.png
Cxx-Ry-pure-env.png      # 仅路线 A 和路线 C
Cxx-Ry-prompt.txt
Cxx-Ry-run.json
Cxx-Ry-review.json
```

- `Cxx`：概念编号（C01-C04）
- `Ry`：路线编号（RA-RD）

### 成功判定

一条路线在一个概念上成功，当且仅当：
- 自动指标：地标位置 Δ < 0.05，SSIM > 0.75，色板 ΔE < 15
- 人工评分：五个维度均分 ≥ 4/5

一条路线整体通过：4 个概念中至少 3 个成功。

### 失败分类

- `env_inconsistent_structure`：地标位置、形状、尺度明显不同
- `env_inconsistent_lighting`：光照方向、色温、时间明显不同
- `env_inconsistent_style`：画风、笔触、材质漂移
- `pet_placement_failed`：宠物未出现或位置完全错误
- `pet_identity_drift`：宠物身份、比例、风格不一致
- `inpaint_background_damage`：路线 A/C 的 inpainting 破坏了环境
- `reference_over_constrained`：路线 B 过度保留了宠物状态1
- `provider_failure`：请求失败或结果无法解码

## Testing Decisions

### 外部可观察行为

1. 两个场景的主要地标（灯塔、桥、树）必须在相同位置、相同视角下可辨认为同一物体。
2. 光照条件（时间、主光方向、色温）必须一致。
3. 画风、色板、材质处理必须一致。
4. 宠物必须成功出现且身份可辨认。
5. 宠物状态（位置、姿态、动作）必须明显不同。

### 对照与消融

- 每个概念额外生成一张"无宠物纯环境图"作为视觉基线（非路线生成结果，单独调用）。
- 路线 A 和路线 C 的纯环境母版与基线对比，判断"无宠物环境"的生成质量。

### 与已有验证的关系

- pilot4mvp3 已证明 image-2 的固定尺寸 Mask 不稳定，因此路线 C 预期失败或需要修正。
- Session 0 已确认 image-2 的基线生成、图片编辑、多图合成和 Mask 局部编辑能力。
- 本 Spec 新增验证"双场景环境一致性"这一产品需求，不重复验证单张图片质量。

## Out of Scope

- LLM 自动规划宠物位置和状态；本 Spec 使用人工冻结的宠物状态描述。
- Unity 坐标转换和点击区域生成。
- 视频生成、音频生成。
- 多于两个场景的一致性。
- 完全自动化的一致性修复或返修。
- 生产级编排、重试、并发。

## Further Notes

本实验的关键分界是：

```text
单张场景好看 ≠ 两张场景环境一致
环境一致 ≠ 宠物自然融入
技术指标通过 ≠ 视觉可接受
```

如果路线 A 的 inpainting 破坏背景，应考虑路线 B 或寻找更精确的 inpainting 模型。  
如果路线 B 的参考图过度保留宠物，应调整 Prompt 或探索局部 Mask 参考。  
如果路线 C 失败但路线 A 成功，说明程序化 Mask 本身不是问题，而是 Mask 稳定性。  
如果所有路线都失败，说明当前技术条件下"共享环境"不可行，需要降级为"相似风格"。

## 预期产物

- `dual-scene-validation-spec.md`：本规格（已完成）
- `experiment-runner.py`：实验执行脚本
- `outputs/Cxx-Ry-*/`：每组实验的输出图片、Prompt、运行日志
- `reviews/consistency-measurements.json`：自动指标测量结果
- `reviews/manual-review.csv`：人工评审表
- `reviews/conclusion.md`：最终结论和推荐路线
