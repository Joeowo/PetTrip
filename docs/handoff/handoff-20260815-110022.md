# Handoff：pilot4mvp 会话 3 完成 → 下一步会话 4

- 日期：2026-08-15
- 分支：`feat/pilot4mvp1`（worktree `.claude/worktrees/pilot4mvp-phase3`，已 checkout 此分支）
- 上一步：会话 3 全部通过并合并（merge commit `1ce6195`，含 `65915d5` + `d3e0d98`）
- 下一步：**会话 4：Unity -> 报告/SQLite -> 重放**（四个会话的最后一个）

## 状态一览（详细记录见各工件，此处不重复）

| 事项 | 状态 | 证据/记录位置 |
| --- | --- | --- |
| 会话 1（人工 Snapshot -> Unity） | ✅ 通过 | `pilot4mvp/four-session-pilot-spec.md`；`runs/pilot-20260812-001/` |
| 会话 2（Python 服务 -> Unity HTTP） | ✅ 通过 | `runs/session2-unity/README.md` |
| 会话 3（外部模型 -> 服务 -> Unity） | ✅ 通过 | spec 内"会话 3 通过记录（2026-08-15）"；`runs/session3-20260815-023543-bcb8/`（`content-ready.json` 标记）；`runs/session3-unity/README.md` |
| 会话 4（Unity -> 报告/SQLite -> 重放） | ⬜ 未开始 | spec"会话 4"小节 + `connectivity-checklist.md` 剩余 5 个未勾项 |

会话 3 收尾要点（详细在 spec 与 README）：真实付费管线一次通过（原生
`/responses` Structured Outputs WorldSpec + `gpt-image-2` 1774x887→512x288），
Unity PlayMode 3/3 通过（新增 `Session3HttpLoadingTests`），54+18 测试通过，
凭证扫描干净。

## 环境与操作事实（repo 内无记录，新会话必读）

1. **Python 环境**：phase3 worktree 自身无 `.venv`。测试与服务统一使用
   `C:/Users/Mechrevo/Documents/ChatGPT/PetTrip/.claude/worktrees/session2-content-service/pilot4mvp/.venv/Scripts/python.exe`
   （Python 3.12.13，与 `pilot4mvp/session3/requirements.txt` 版本一致，含 opencv 5.0.0）。
2. **pytest 必须分目录单跑**：session2 与 session3 的包都叫 `content_service`，
   conftest 各自插 sys.path；合并运行会因 `sys.modules` 缓存冲突而收集失败。
   分别执行 `pytest pilot4mvp/session2/tests` 与 `pytest pilot4mvp/session3/tests`。
3. **凭证**：worktree 根目录 `.env`（gitignored）已就绪，含 `RESPONSES_*` 与
   `IMAGES_*` 六个变量（Responses=api.denxio.com/gpt-5.6-sol，Images=5202828.xyz/v1/gpt-image-2）。
   只从环境变量读取，不写入日志/产物。
4. **付费入口**：`pilot4mvp/session3/run_paid_pipeline.py --confirm-paid`
   （真实 Responses+Images 调用，需显式确认才执行）。
5. **Unity**：Editor `C:\Program Files\Unity\Hub\Editor\6000.3.21f1`；工程
   `unity/PetTrip`；PlayMode 编排参考 `pilot4mvp/session3/run_unity_session3.py`
   （防假通过：端口归属、run_id 一致、XML 定位用例、截图只认本次新生成）。
   Unity 消费端复用 `Session2Beach` 通用 HTTP 场景（127.0.0.1:8000）。
6. **Git 布局**：`main` 仍在会话 2 状态（`b34a977`）；`feat/pilot4mvp1` 是
   pilot4mvp 第一期汇总分支（会话 1-3）；主仓库目录 checkout 在
   `feat/pilot4mvp2-agent`（另一个已完成的试点，勿混淆）。用户工作流：worktree +
   专用分支，review 通过后明示才合并 main（见记忆 pilot-worktree-workflow）。

## 会话 4 待办（完整定义见 spec，此处仅导航）

1. 前置检查：`runs/session3-20260815-023543-bcb8/` 存在且 content-ready ✅；
   **SQLite 可用性未验证**（checklist"开始前检查"仅剩此一项未勾）——会话 4 第一件事。
2. Unity 触发 `pet_wave`、放置 `small_shelter`、保存 Snapshot v2（对应
   checklist 第 8、9 项）。
3. Unity 生成验证报告 POST 回 FastAPI，写 SQLite（第 10 项）；报告的 `run_id`
   与 Snapshot 哈希一致。
4. 重启服务，同 `run_id` 离线重放 Snapshot，不再调用 Responses/Images（第 11 项）。
5. 通过门槛与负例（4xx 不创建运行目录等）见 spec"会话 4"小节与 Testing Decisions。
6. 完成后：更新 spec 通过记录 + checklist 勾选 + runs 证据 README，提交到
   `feat/pilot4mvp1`。

## 建议下个会话使用的方式

- 无需特定 skill；沿用标准工作流（TaskCreate 跟踪、Bash 跑测试/编排）。
- 遵守 spec 全局规则：缺资源先申请再停，不 Mock、不跳步、不手改失败 JSON。
- 记忆 `pilot4mvp-session3-env` 已包含本文档第 1、2、5、6 点的摘要，可双保险。
