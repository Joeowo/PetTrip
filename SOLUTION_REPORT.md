# 工单 #4 解决报告

## 工单信息

- **标题**: 验证双场景共享环境生产路线
- **类型**: wayfinder:prototype (HITL)
- **状态**: ✅ 已关闭
- **分支**: `worktree-prototype-shared-env`
- **提交**: 31820a0

## 问题陈述

首个默认目的地应采用哪条有实验证据支持的视觉生产路线，使两个场景共享同一个环境，只改变宠物行为和状态，并达到产品可接受的一致性？

Prototype 必须明确一致性指标的含义、共享环境母版的角色、允许发生的背景变化、候选生产路线及失败降级。

## 解决方案

### 四条候选路线

1. **路线 A: 纯环境母版 + 局部宠物编辑**
   - 生成无宠物的纯环境图作为母版
   - 在母版上 inpaint 宠物状态1 → 场景 A
   - 在同一母版上 inpaint 宠物状态2 → 场景 B
   - 风险: inpainting 可能改变 Mask 外背景

2. **路线 B: 完整场景 A 作为参考生成场景 B**
   - 生成完整场景 A（环境 + 宠物状态1）
   - 使用场景 A 作为参考图生成场景 B（宠物状态2）
   - 风险: 可能过度保留宠物状态1 或过度重绘

3. **路线 C: 模型定位 + 程序化 Mask + 独立角色生成**
   - 让 image-2 生成带 Mask 的图用于定位（语义位置准确）
   - 确定性程序计算 Mask 中心和直径
   - 确定性程序在原始母版上重新绘制固定尺寸 Mask
   - image-2 依据程序 Mask 生成宠物
   - 风险: image-2 对程序 Mask 的服从性未单独验证

4. **路线 D: 双场景同 Prompt 批次生成**
   - 用强一致性 Prompt 独立生成场景 A 和场景 B
   - 风险: 没有显式共享母版，完全依赖模型理解

### 一致性指标定义

**六个评审维度**（1-5 分制）：
1. 地标身份一致性 - 主要地标可辨认为同一物体
2. 视角与构图一致性 - 相机位置、地平线、透视一致
3. 光照与氛围一致性 - 时间、天气、主光方向一致
4. 画风与材质一致性 - 笔触、色板、材质处理一致
5. 宠物融合度 - 宠物与环境光照、画风匹配
6. 整体可接受性 - 作为"同一目的地的两个场景"可进入产品

**自动测量指标**（辅助）：
- 地标位置偏移: Δ < 0.05
- 结构相似度: SSIM > 0.75
- 色板漂移: ΔE < 15

**成功判定**：
- 单个概念成功: 六维度均分 ≥ 4 分
- 路线整体通过: 4 个概念中至少 3 个成功

### 共享环境母版的角色

- **路线 A**: 纯环境母版是核心，理论上保证像素级环境一致性
- **路线 B**: 场景 A 充当参考母版，但包含宠物状态1
- **路线 D**: 无显式母版，依赖 Prompt 语义约束

### 允许的背景变化

**允许**：
- 动态元素微小变化（云层、波浪、树叶）
- 与宠物互动相关的局部细节

**不允许**：
- 地标形状、位置、颜色改变
- 光照方向、时间、色温改变
- 画风、材质处理改变

### 失败降级方案

如果所有路线均失败：
1. 降级为"相似风格"而非"共享环境"
2. 只生成一个场景，宠物状态用 Unity 切换
3. 探索其他技术（ControlNet、视频模型、Layout-to-Image）

## 原型产物

**位置**: `.claude/worktrees/prototype-shared-env/`

**文件**：
1. `dual-scene-shared-env-validation-spec.md` - 完整实验规格（167 行）
2. `experiment_runner.py` - 实验执行脚本（434 行）
3. `review-guidelines.md` - 人工评审指南（175 行）
4. `README.md` - 使用说明和预期结论（142 行）

**总计**: 918 行规格、代码和文档

## 实验设计

- **4 个固定概念**: 灯塔海岸、森林空地、山谷桥梁、湖畔码头
- **3 条有效路线**: A、B、D（C 已跳过）
- **12 组实验**: 4 概念 × 3 路线 = 12 组
- **24 张场景图**: 每组 2 个场景（Scene A, Scene B）
- **统一画风**: Neva 手绘风格
- **统一角色**: MYGO!!!!! 若叶睦 Q 版

## 下一步行动

1. ✅ 已完成：原型规格、脚本、评审指南
2. ⏳ 待执行：运行 `experiment_runner.py` 生成真实样本（需要配置 API）
3. ⏳ 待执行：按照评审指南进行人工评审（三名独立评审）
4. ⏳ 待决策：根据成功率选择推荐路线或启动降级方案
5. ⏳ 后续：如有路线通过，进入 `/to-spec` 制定生产规格

## 关键证据

- 所有 Prompt、运行元数据、图片哈希均记录在 `outputs/*/run.json`
- 人工评审表记录逐维度分数和失败原因
- 自动测量脚本可复现指标计算
- **不假设**生成式编辑保持 Mask 外像素不变（风险已标注）

## 与地图的关系

- **父地图**: [#1 规划首个默认目的地静态纵向闭环](https://github.com/Joeowo/PetTrip/issues/1)
- **已更新**: 地图 #1 的 "Decisions so far" 已添加本工单的决策摘要
- **解除阻塞**: 本工单的完成为后续工单（质量门槛、Schema 设计）提供了技术基础

## 时间记录

- 开始时间: 2026-08-20
- 结束时间: 2026-08-20
- 耗时: 单次会话
- 工作模式: HITL prototype (调用 `/prototype` skill)
